import dotenv from 'dotenv';
dotenv.config();

export const getStaticProps = async () => {

}

const Courses = () => {
    return ( 
        <h2>These are all the courses</h2>
     );
}
 
export default Courses;